# Render deployment configuration for Mom Academy Backend
# Build Command: dotnet publish -c Release -o out
# Start Command: dotnet out/MyApi.dll

# Environment Variables needed on Render:
# DATABASE_URL=postgresql://neondb_owner:npg_jObtF4Ke1lkz@ep-divine-glade-adk94w1g-pooler.c-2.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
# PORT=10000 (Render will set this automatically)
# ASPNETCORE_ENVIRONMENT=Production

# Health Check URL: /health
# API Documentation: /swagger